<aura:application>
    <!-- Aura If is here to ensure we have client side dependencies and so Aura on the Client gets initialized. Otherwise this app would just be a plain HTML page. -->
    <aura:if isTrue="{!true}">
        FIRST KRIS!
    </aura:if>
</aura:application>